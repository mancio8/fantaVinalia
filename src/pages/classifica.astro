---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";
import { getCollection } from "astro:content";
import createSlug from "../lib/createSlug";
import Accordion from "../components/AccordionTeam.astro";
import AccordionTeam from "../components/AccordionTeam.astro";

const posts = (await getCollection("blog")).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);

const last_posts = posts.slice(0, 3);
---

<BaseLayout sideBarActiveItemID="classifica">
  <div class="pb-12 mt-5">
    <div class="py-2">
        <text class="text-lg">
            Benvenuti alla pagina ufficiale delle classifiche di Fantavinalia, il gioco che celebra il talento e la creatività degli artisti partecipanti! In questo spazio emozionante, vi condurremo attraverso un viaggio affascinante nel mondo delle espressioni artistiche, mettendo in luce i protagonisti che hanno catturato l'immaginazione di tutti noi.
          </text>
    </div>
    <div class="mt-8"></div>
  </div>
  <div class="divider divider-success"></div>
  <AccordionTeam
  nomeArtista="Geolier"
  punti="15"
    
  />
  <AccordionTeam
  nomeArtista="Geolier2"
  punti="0"
    
  />
  
</BaseLayout>
